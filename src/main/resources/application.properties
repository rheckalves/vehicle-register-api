spring.jpa.properties.hibernate.globally_quoted_identifiers=true

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

server.port = 8080

# server.port = ${PORT}

spring.jpa.database=POSTGRESQL
spring.datasource.platform=postgres
spring.database.driverClassName=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/car-register
spring.datasource.username=postgres
spring.datasource.password=itd4nmf0
spring.jpa.hibernate.ddl-auto=update

# Para inserir dados utilize o valor {always} ou {never} para não inserir
spring.datasource.initialization-mode=never

# Spring Security

security.oauth2.client.scope=password
security.oauth2.client.client-id=admin
security.oauth2.client.client-secret=admin